// import { Server } from "socket.io";

// type EventHandler = (socket: Server) => void;
// const event_handlers: { [key: string]: EventHandler } = {};

// export const init = (socket: Server) => {
//   for (const [event, handler] of Object.entries(event_handlers)) {
//     socket.on(event, handler);
//   }
// };
